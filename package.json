{
  "name": "@codeplaydata/datasus",
  "version": "1.3.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && copy LICENSE dist\\LICENSE && copy README.md dist\\README.md && copy package.json dist\\package.json",
    "build:lib": "tsc -p tsconfig.lib.json && copy LICENSE dist\\LICENSE && copy README.md dist\\README.md && copy package.json dist\\package.json",
    "build:siasus": "tsc -p tsconfig.app.json",
    "publish": "npm run build:lib && npm publish ./dist --access=public",
    "siasus:main": "npm run build:siasus && node --max-old-space-size=4096 ./dist/app/siasus/main.js",
    "siasus:linkage": "npm run build:siasus && node --max-old-space-size=4096 ./dist/app/siasus/linkage.js",
    "test": "npx tsx test.ts"
  },
  "keywords": [
    "DATASUS",
    "dbc",
    "health"
  ],
  "author": "Pedro Paulo dos Santos (dr2p) - https://github.com/dr2pedro",
  "license": "Apache-2.0",
  "description": "This application decompress the datasus micro data and serve as a gateway class.",
  "devDependencies": {
    "@types/node": "^22.15.23",
    "mongodb": "^7.0.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@codeplaydata/dbc2dbf": "^1.0.2",
    "basic-ftp": "^5.0.5",
    "dbffile": "^1.12.0"
  }
}